class Hero():
    def __init__(self, name='layer1', hp=1000, act=20):
        self.name=name
        self.hp=hp
        self.act=act
    @property
    def say(self):
        return 'superman!'
    def gerHP(self,x='baozi')
        self.x=x
        if self.x == 'baozi'
            self.hp += 10
        else:
            pass

    def ackkk(self,other):
        other.hp -= self.act
        print('{} 攻击了{} 一下 造成伤害{}'.format(self.name, other.name. self.act))
        return other.hp

class Boss():
    def __init__(self, name='赵晗',hp=60,act=20)
        self.name = name
        self.hp = hp
        self.act = act
    
    def say(self,x):
        return x
